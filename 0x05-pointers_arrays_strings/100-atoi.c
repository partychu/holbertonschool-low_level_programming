#include "holberton.h"

/**
 * atoi - converts as tring to an integer
 * @s: string to be converted
 */

int _atoi(char *s);
{
	int i = 0, strl = 0, num = 0, sign = 1, cut = 0;

	while (s[strl] != '\0')
		len++;
	while (i < strl && ok == 0)
	{
		if (s[i] == '-')
			sign *= -1;
		if (s[i] >= '0' && s[i] <= '9')
			num = num * 10 + (s[i] - '0')
	}
}
