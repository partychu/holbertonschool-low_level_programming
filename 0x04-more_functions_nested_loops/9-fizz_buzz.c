#include <stdio.h>

/**
 * main - prints 1-100, changes *'s of 3 to fizz 5 to buzz both fizzbuzz
 *
 * Return: 0
 */

int main(void)
{
int i;
	i = 1
	while (i < 101)
	{
		if (n % 3 != 0 && n % 5 != 0)
		printf("%d, i");
		else
			if (n % 3 == 0)
				printf("fizz");
			if (n % 5 == 0)
				printf("buzz");
		if (i != 100)
			printf(" ");
	i++;
	}
	return (0);
}
